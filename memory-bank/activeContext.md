# Active context

- Workspace: `d:\ROCRE\softwares\Zoho Books Mapping Studio` (mirror for `github.com/rajeshkamalwar/Zoho-mapping-studio`).
- Initial implementation landed: backend API, Alembic `001_initial_schema`, seed Walmart profile, unified publisher, Celery task, React Mapping Studio.
- Immediate ops: create Postgres DB, `alembic upgrade head`, `POST /api/admin/seed-walmart-profile`, set Zoho env for real publishes.
